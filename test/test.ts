import { Feature, Scenario, Given, When, Then } from '@/index'

Feature('smoke tests', () => {
  Scenario('can create scenario', () => {
    Given('nothing', () => {})
    When('nothing', () => {})
    Then('expect nothing', () => {})
  })
})
